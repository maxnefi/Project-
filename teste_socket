#include <stdio.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <netdb.h>

int main()
{
   //Criação de um socket e que valor ele retorna
	int meusocket;
	meusocket= socket(AF_INET,SOCK_STREAM,0);
	printf("%d\n", meusocket);
  //
  
	return 0;
}
